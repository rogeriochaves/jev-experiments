shorthand.mdx  (both rules, 6 sections, threshold 0.7)

## Why the statement is handed back  [163 words]
! 1.00  docs/artifact-verbs-literal  Rule 17: artifact as subject of a cognitive, transport or posture verb (literal shape)
        > The statement carries two parameters, `start_at` and `end_at`, so a rerun has to supply both:
! 0.89  docs/filler-sentence  What "less bs" means: no honest-but-empty sentences
        > That is deliberate: the shorthand is a way into the query language rather than a replacement for it, so the first thing it gives you is the query it wrote.
! 0.78  docs/heading-indirect-form  Rule 29: headings are the question the reader types
        > ## Why the statement is handed back
! 0.76  docs/indefinite-event-opener  Rule 26: do not open a sentence with an indefinite event
        > A run always executes one LangWatchQL statement.
  0.66  docs/restated-tool-guidance  Rule 23: if the product guides, the docs do not restate the guidance
        > That is deliberate: the shorthand is a way into the query language rather than a replacement for it, so the first thing it gives you is the query it wrote.
  0.60  docs/concept-opener  Rule 15: open a concept page with the definition of its noun
        > A run always executes one LangWatchQL statement.
  0.59  docs/artifact-as-subject  Rule 17: give the verbs to the reader
  0.51  docs/heading-unanswered  Rule 15: the first sentence answers the heading

## What each target expands into  [207 words]
  0.51  docs/artifact-as-subject  Rule 17: give the verbs to the reader

## The token budget  [109 words]
  0.57  docs/paraphrased-artifact  Rule 2: show the real artifact, always
  0.50  writing/passive-voice  Active voice

## What a shorthand filter can ask for  [294 words]
! 1.00  docs/artifact-verbs-literal  Rule 17: artifact as subject of a cognitive, transport or posture verb (literal shape)
        > The refusal names the field it rejected and lists the fields it accepts.
! 0.90  docs/artifact-as-subject  Rule 17: give the verbs to the reader
        > `--filter` speaks the same language as the trace explorer's search bar, and a shorthand supports the half of it the LangWatchQL trace view can answer:
  0.51  docs/heading-unanswered  Rule 15: the first sentence answers the heading

## Where the filter lands  [71 words]
! 0.85  docs/artifact-as-subject  Rule 17: give the verbs to the reader
        > Over `threads` and `llm-spans` it is a subquery on the trace view, because neither the metrics view nor the span view carries the trace's attributes.

## Sending a statement instead  [83 words]
  0.67  docs/artifact-as-subject  Rule 17: give the verbs to the reader
        > A request carrying both `sql` and `target` is refused, and so is one carrying neither. `--param` belongs to a statement of your own: a target writes its own parameters, so there is none of yours left to fill.
  0.50  writing/passive-voice  Active voice

cost: 52,834 input tokens over 10 requests = USD 0.0022 (0.042 USD per million)

